<button type="button" data-route="{{ route('admin.draft_orders.send_to_order') }}" draft-data="{{ $draft_order->id }}"
    ref-data="{{ $draft_order->tracking_code }}" class="btn btn-sm btn-outline-primary convert"> تبدیل به سفارش</button>
